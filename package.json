{
  "name": "gpt-image-2-local-pwa",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first PWA for GPT Image reference-image generation",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check scripts/check-pr-summary.mjs && node --check scripts/smoke-check.mjs",
    "smoke": "node scripts/smoke-check.mjs",
    "pr:summary": "node scripts/check-pr-summary.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "multer": "^2.0.2",
    "openai": "^6.20.0"
  }
}
